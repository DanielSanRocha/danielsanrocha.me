{
  "name": "danielsanrocha.me",
  "version": "1.0.0",
  "description": "My personal Home Page =)",
  "author": "danielsantanarocha@gmail.com",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --remote=static --cleanup --force"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^1.2.7",
    "cross-env": "^5.2.0",
    "nuxt": "^2.4.0"
  },
  "devDependencies": {
    "node-sass": "^4.12.0",
    "nodemon": "^1.18.9",
    "sass-loader": "^7.1.0"
  }
}
