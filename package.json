{
  "name": "danielsanrocha.com",
  "version": "1.0.0",
  "description": "My personal Home Page =)",
  "author": "danielsantanarocha@gmail.com",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "clean": "rm -rf dist",
    "build": "nuxt generate",
    "push": "push-dir --dir=dist --branch=master --remote=static --cleanup --force",
    "deploy": "npm run clean && npm run build && npm run push"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.13.0",
    "@tensorflow/tfjs": "^1.2.7",
    "cross-env": "^5.2.0",
    "nuxt": "^2.4.0",
    "showdown": "^1.9.1",
    "vuejs-paginate": "^2.1.0"
  },
  "devDependencies": {
    "node-sass": "^4.12.0",
    "nodemon": "^1.18.9",
    "sass-loader": "^7.1.0",
    "push-dir": "^0.4.1"
  }
}
